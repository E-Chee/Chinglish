<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evan.languageapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeScreenActivity"
            android:label="@string/title_activity_home_screen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GreetingsLessonActivity" />
        <activity android:name=".GreetingsFlashcardActivity" />
        <activity android:name=".MatchMediumActivity" />
        <activity android:name=".LessonOneActivity" />
        <activity android:name=".MatchHardActivity" />
        <activity android:name=".LessonTwoActivity" />
        <activity android:name=".LessonThreeActivity" />
        <activity android:name=".LessonFourActivity" />
        <activity android:name=".LessonFiveActivity"></activity>
    </application>

</manifest>